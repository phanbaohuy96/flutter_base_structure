# fl_utils

UI-independent Flutter helpers for this template: extensions, date/number/phone formatting, JWT decoding, encryption, debouncing, QR payment parsing, and other shared utilities.

Import via `package:core/core.dart` (re-exported) or directly via `package:fl_utils/fl_utils.dart`.

See [`lib/src/`](lib/src/) for the full list of helpers.
